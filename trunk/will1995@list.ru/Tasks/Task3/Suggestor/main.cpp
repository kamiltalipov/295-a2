#include <iostream>
#include <stdio.h>
#include <vector>
#include <string>

using namespace std;

struct elem
{
	string s;
	int min, add;
};

class comparator
{
public:
	bool operator () (elem &a, elem &b)
	{
		ololo
	}
};

class Suggezztor
{


};

int main()
{
	freopen("input.txt", "r", stdin); //input+dictionary+frequences.
	string s;
	int k;
	cin >> s;
	cin >> k;
	int fr;
	string tmp;
	while (cin >> tmp)
	{
		cin >> fr;
	}

	return 0;
}