1. насчет set<int>::begin(), скорее всего ассимптотика O(1) - если хранить для каждого сета указатель на наименьший эллемент, но, если он написан без запоминания, то ассимптотика может достигать O(logN). Оператор ++ выполняется в худшем случае за 2 прохода на всю глубину дерева, то есть за O(logN).

2.
map < T1, T2 >::iterator it = mp_name.find(key_name);
if (it == mp_name.end())
	//Ключа не было
mp_name[key_name] = value;

3. set < T, std::greater < T > > set_name;

4. queue < int > q(list_name.begin(), list_name.end());

5. vector_name.capacity() - vector_name.size()

6. list1.splice(list1.end(), list2);

7. map_name["abcd"] = BadCopy(5);